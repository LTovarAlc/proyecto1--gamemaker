// create
opened = false;

// (cerrado)
sprite_closed = spr_chest_closed;

// (abriéndose)
sprite_opening = spr_chest_opened;

// sprite inicial (cerrado)
sprite_index = sprite_closed;

image_index = 0;
image_speed = 0; // Detener la animación